package it.unipi.largescale.pixelindex;

import it.unipi.largescale.pixelindex.controller.ApplicationController;
import it.unipi.largescale.pixelindex.model.UnregisteredUser;

public class Main {
    public static void main(String[] args) {
        UnregisteredUser unregisteredUser = new UnregisteredUser();
        ApplicationController applicationController = new ApplicationController();
    }
}
